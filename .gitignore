# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Test & tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
coverage.xml

# Environment / secrets
.env
.env.*
!.env.example

# Local data & artifacts
mlruns/
mlartifacts/
reports/
eval/results.json
*.db
*.sqlite3
data/**/*.parquet
data/**/*.csv
!data/.gitkeep

# Notebooks
.ipynb_checkpoints/

# Personal / interview prep — not part of the project docs.
# Anything dropped in ignore/ stays local. Kept as a folder rather than a growing list of
# filenames so a new personal file is protected the moment it lands, instead of the first
# time someone remembers to add a line here.
ignore/
# Legacy entries, kept in case a copy reappears at the repo root.
presentation.md
deep_dive_cheatsheet.html

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
