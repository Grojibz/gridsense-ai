apiVersion: v1
kind: Service
metadata:
  name: gridsense-api
  namespace: gridsense
  labels:
    app.kubernetes.io/name: gridsense-api
    app.kubernetes.io/part-of: gridsense
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gridsense-api
  ports:
    - name: http
      port: 80
      targetPort: http
